{
  "name": "herdr-codex-app",
  "version": "0.1.6",
  "private": true,
  "description": "Automatically sync recent Codex chats and projects into Herdr.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "preflight": "node scripts/preflight.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "license": "Apache-2.0"
}
